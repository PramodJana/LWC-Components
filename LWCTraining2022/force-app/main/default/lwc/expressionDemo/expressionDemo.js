import { LightningElement } from 'lwc';

export default class ExpressionDemo extends LightningElement {
    //declare memeber variable in this class
    //Rather than calling as member variables call them as Fields or Property
    //Field names are always in camalCase
    greet = 'Howdy LWC!!!!';
}